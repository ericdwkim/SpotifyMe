events {
    worker_connections 1024; # set to default
}

# http {
    # server { # pgadmin reverse-proxy
    #     listen 80;
    #     server_name localhost:5000/pgadmin;

    #     # pass requests for dynamic content to flask/SQLAlchemy
    #     location / {
    #         proxy_pass http://127.0.0.1:8080;
    #     }
    # }
    # upstream flask {
    #     server web:5000;
    # }
http {


# upstream flask {
#     server web:5000;
# }


    server { # load balancing
        listen 80;

        location / {
            resolver 127.0.0.11 valid=30s;
            set $upstream "web:5000";
            proxy_pass http://$upstream;
            # proxy_pass http://flask:5000;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header Host $host;
            proxy_redirect off;
        }

        location /static/ {
            alias /usr/src/flask/static/;
        }
    }
}